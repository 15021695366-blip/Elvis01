# Elvis01

零壹公社官网项目，基于 React + Vite + Tailwind CSS 4。

## 本地运行

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
npm run preview
```

## 部署到 GitHub Pages

仓库名固定为 `Elvis01`，`vite.config.ts` 已配置：

```ts
base: "/Elvis01/"
```

推送到 `main` 分支后，GitHub Actions 会自动部署。 
update
